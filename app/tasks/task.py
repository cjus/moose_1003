import datetime
print("===================================================", flush=True)
print("Executing (Python) job3", flush=True)
current_time = datetime.datetime.now()
print(f"Current date and time: {current_time}", flush=True)
print("Job3 completed successfully", flush=True)
print("===================================================", flush=True)
